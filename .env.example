# Postgres
POSTGRES_USER=portfolio
POSTGRES_PASSWORD=portfolio
POSTGRES_DB=portfolio
POSTGRES_DB_STAGING=portfolio_staging
POSTGRES_PORT=5432

# Backend (production profile)
CORS_ORIGIN=http://localhost:8080
GITHUB_USERNAME=octocat
GITHUB_TOKEN=
GITHUB_CACHE_TTL_SECONDS=300

# Resend (https://resend.com) — powers contact-form notification emails, shared by both profiles.
# Required at boot (Joi validation) — fill these in before starting the backend.
RESEND_API_KEY=
CONTACT_FROM_EMAIL="onboarding@resend.dev"
CONTACT_NOTIFICATION_EMAIL="you@example.com"

# Admin dashboard auth. JWT_SECRET is required at boot (Joi validation, min 32
# chars) — generate one with `openssl rand -base64 32`. JWT_EXPIRES_IN_SECONDS
# defaults to 7200 (2h) if unset.
JWT_SECRET=
JWT_EXPIRES_IN_SECONDS=7200

# Backend (staging profile)
CORS_ORIGIN_STAGING=http://localhost:8081
JWT_SECRET_STAGING=

# Frontend. Baked in as the build-time fallback, but also read at container
# startup — changing this and running `docker compose up -d` (no `--build`)
# is enough to point a deployed frontend at a different API, no rebuild
# needed. See claude/architecture.md.
VITE_API_BASE_URL=http://localhost:3000
VITE_API_BASE_URL_STAGING=http://localhost:3001
